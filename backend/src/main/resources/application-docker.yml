spring:
  application:
    name: hotel-backend
  datasource:
    url: jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
    username: ${DB_USER:user}
    password: ${DB_PASSWORD:password}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  graphiql:
    endpoint: /graphql
    enabled: true
server:
  port: 8082
grpc:
  server:
    port: 9099
logging:
  org.springframework.web: TRACE

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics, prometheus
  prometheus:
    enabled: true

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS}
